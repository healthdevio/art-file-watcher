{
  "compilerOptions": {
    /* Estrutura do Projeto */
    "target": "ES2022" /* Especifica a versão alvo do JavaScript. */,
    "module": "CommonJS" /* Usa o módulo CommonJS, compatível com o pkg. */,
    "moduleResolution": "node" /* Define como o TypeScript deve resolver módulos. */,
    "rootDir": "./src" /* Especifica a pasta raiz dos arquivos TS. */,
    "outDir": "./dist" /* Especifica o diretório de saída para os arquivos JS compilados. */,
    "esModuleInterop": true /* Permite a importação de módulos CommonJS usando a sintaxe de importação padrão. */,

    /* Configurações de Tipagem e Rigor */
    "strict": true /* Habilita todas as opções de verificação de tipo rigorosa. */,
    "skipLibCheck": true /* Pula a verificação de tipo dos arquivos de definição de biblioteca. */,
    "forceConsistentCasingInFileNames": true /* Garante que o uso de maiúsculas e minúsculas nos nomes de arquivos seja consistente. */
  },
  "include": ["src/**/*.ts", "__tests__/**/*.ts"],
  "exclude": ["node_modules", "scripts/**/*.mjs", "dist", "bin", "coverage"],
  "ts-node": {
    "esm": true
  }
}
